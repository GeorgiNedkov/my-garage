extends ../_layout


block content
    h1 Find your new car
    
    form(action="/cars/car-search" method="POST")
        .form-group
            label
                | Model:
                select(name="Model")
                    each mod in Models
                        option=mod

        //- .form-group
        //-     label
        //-         | Mileage:
        //-         input(name="Mileage1")
        //-         |-
        //-         input(name="Mileage2")
        //-         |km
        //- .form-group
        //-     label
        //-         | Price:
        //-         select(name="Price1")
        //-             each price in prices
        //-                 option=price
                    
        //-         |-
        //-         select(name="Price1")
        //-             each price in prices
        //-                 option=price
        //-         | €
        .form-group
            label
                | Year:
                input(name="Year")

        button
            | Search

    ul.list
        each car in model
            li.car-item
                a(href="/cars/" + car.id)
                    if car.imgUrl
                        img(width=100 src=car.imgUrl)
                    else
                        img(width=100 src="http://www.bmw.co.uk/vc/resources/img/no_img.png")
                    span=car.Model