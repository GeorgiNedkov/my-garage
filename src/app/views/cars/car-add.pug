extends ../_layout

block content
    //- link(rel="stylesheet", href="/static/js/validator.js")
    form(action="/cars" method="POST" id="signForm" style='margin-bottom: 20px; border: 1px solid #ddd; padding: 20px; width: 50%; margin: 0 auto 20px;')
        .form-group
            label
                | Makes:
                select(name="Makes")
                    each mod in model
                        option=mod
        .form-group
            label
                | Mileage:
                input(name="Mileage")
                |km
        .form-group
            label
                | location:
                input(name="location")  
        .form-group
            label
                | Price:
                input(name="Price")
                |€
        .form-group
            label
                | Year:
                input(name="Year" )
        .form-group
            label
                | description:
                textarea(name="Description" width=100 height=100)  
        .form-group
            label
                |Image:
                input(name="imgUrl")
                                //- input(name="imgUrl") 
        .form-group
            label
                |Telephone number:
                input(name="Telephone")
                                //- input(name="imgUrl")        
        button
            | Save