extends ../_layout

block content
    form(action="/cars" method="POST" style='margin-bottom: 20px; border: 1px solid #ddd; padding: 20px; width: 50%; margin: 0 auto 20px;')
        .form-group
            label
                | Makes:
                select(name="Makes")
                    each mod in model
                        option=mod
        .form-group
            label
                | Mileage:
                input(name="Mileage")
                |km
        .form-group
            label
                | Price:
                input(name="Price")
                |€
        .form-group
            label
                | Year:
                input(name="Year")
        .form-group
            label
                | description:
                textarea(name="description" width=100 height=100)  
        .form-group
            label
                |Image:
                input(name="imgUrl" multiple)
                                //- input(name="imgUrl") 
        button
            | Save