extends ../_layout

block content
    form(action="/cars" method="POST")
        .form-group
            label
                | Model:
                select(name="Model")
                    option volvo
                    option BMV
                    option mercedes
                    option Audi
        .form-group
            label
                | Mileage:
                input(name="Mileage")
        .form-group
            label
                | Color:
                input(name="Color")
        .form-group
            label
                | Price:
                input(name="Price")
        .form-group
            label
                | Year:
                input(name="Year")
        .form-group
            label
                | Description:
                textarea(name="description") 
        .form-group
            label
                | imgUrl:
                input(name="imgUrl")     
        button
            | Save